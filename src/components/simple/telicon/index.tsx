import * as React from 'react';
import { View, Text } from 'react-native';

export interface TeliconProps {}

export function Telicon (props: TeliconProps) {
  return (
    <View>
      <Text>Telicon</Text>
    </View>
  );
}
