import * as React from 'react';
import { View, Text } from 'react-native';

export interface ToastTriggerProps {}

export function ToastTrigger (props: ToastTriggerProps) {
  return (
    <View>
      <Text>ToastTrigger</Text>
    </View>
  );
}
